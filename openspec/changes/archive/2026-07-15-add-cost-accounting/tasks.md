## 1. Парсинг данных затрат

- [x] 1.1 Вывести и задокументировать позиционную `RES_SCHEMA` (лист
  `Resources and products`): произведено/брак/полуфабрикаты по продуктам, часы
  сборщиков (доступно/отработано/болезни), станки (эффективность,
  доступно/отработано), закупки/использование сырья в единицах, отгрузки по каналам
- [x] 1.2 Вывести и задокументировать `FIN_SCHEMA` (лист `Financial statements`):
  все статьи накладных и P&L (зарплаты сборщиков/механиков, эксплуатация станков,
  ОТК, транспорт, амортизация, реклама, R&D, персонал, упр.бюджет, страховка,
  кредит-контроль, склад-закупка, маркет-исслед., закупки сырья/п-ф в деньгах)
- [x] 1.3 Реализовать `parse_costs(path)` в `parser.py` (или расширить
  `parse_report`), движок `engine="calamine"`, языконезависимо
- [x] 1.4 Тесты-тождества на всех отчётах: сырьё `Σ(произв−п/ф)×{1,2,3}` == факт;
  станко-часы `Σ(произв−п/ф)×{60,75,120}/эфф` == факт (в пределах ~0.1 %)
- [x] 1.5 Валидировать обе схемы на всех `data/W*.xls` (RU-2026 + разноязычные) и
  сверить суммарную смету с P&L-строкой как контрольную сумму парсинга

## 2. Примитивы себестоимости (`cost.py`)

- [x] 2.1 Модуль констант мануала (Приложение design.md, Табл. 5/6/7/10/11/12/
  13/16/17/18): нормативы сырьё 1/2/3, станко 60/75/120, сборка 100/150/300 +
  фикс. ставки (машино-час 8, техобсл. 85, ОТК 1/ед, амортизация 2.5 % и т.д.)
- [x] 2.2 Функции расхода с подстановкой п/ф: `сырьё = Σ(произв−п/ф)×{1,2,3}`,
  `машино-часы = Σ(произв−п/ф)×{60,75,120}/эфф`, `сборщико-часы = Σ произв×
  {100,150,300}`; инференс МНК только как sanity-check с предупреждением
- [x] 2.3 Рыночные/решенческие цены из отчёта: спот-цена сырья (за лот 1000, курс),
  ставки зарплат сборщика/механика, комиссия дистрибьютора; сглаживание по серии,
  перенос последней цены при нулевой закупке
- [x] 2.4 yield-факторы EWMA по серии: ставка брака, потеря времени сборщиков,
  эффективность станков; вырождение в точку при одном квартале
- [x] 2.5 Применение поправок: брак → делить на годные; болезни → конверсия/ед ↑;
  эффективность → станко-затраты/ед ↑
- [x] 2.6 Реконструкция произв. фиксов по сменам (не прошлый факт): смены =
  `⌈доступ_ст-часы/(станки×576)⌉` (forward — мин. под нужные машино-часы);
  техобслуж = `часы/станок×станки×85`, эксплуатация = `станки×3500+смены×12500+
  ст-часы×8+ед×1`, зарпл. механиков = `(ст-часы+простой)×ставка×(1+премия_смен)×
  4/станок` + избыток по мин. ставке; тест сверки с фактом (техобсл. точно,
  эксплуатация ≤0.5 %)

## 3. Разнесение на 9 ячеек

- [x] 3.1 Объёмы производства из прогноза спроса + желаемый запас (дефолт 0),
  агрегация ячеек в выпуск по продукту
- [x] 3.2 Этап 1: продукт-затраты (материал+п/ф переменные; конверсия — фикс-пул
  по сборщико-минутам `ед_p × время_p`) → в ячейку по единицам `продукт→канал`
- [x] 3.3 Этап 2: канал-пул (дистрибуция/агенты, транспорт, интернет-агент/
  провайдер/веб, гарантия) → на продукты внутри канала по единицам
- [x] 3.4 Overhead: маркетинг + прочие непроизв. накладные → по выручке ячейки
- [x] 3.5 Контроль полноты: Σ отнесённых по 9 ячейкам == суммарные включённые
  затраты (тест на баланс с точностью округления)

## 4. Выходы и API

- [x] 4.1 Полная себестоимость на единицу (absorption) по 9 ячейкам
- [x] 4.2 Contribution margin = цена − материал − п/ф − комиссия дистрибьютора
  (конверсия/накладные исключены)
- [x] 4.3 `cost(current, train, history, ...)` → df по 9 ячейкам + `df.attrs`
  (мета, сверка с P&L, использованные нормативы/факторы), зеркалит `forecast`

## 5. CLI и интеграция в прогноз

- [x] 5.1 Подкоманда `cost` в `cli.py` (имя модуля/команды — `cost`, не `costing`)
  с `--current`/`--train`/`--history`/`--out`; печать таблицы 9 ячеек
- [x] 5.2 `--out` → дублирование таблицы себестоимости в CSV
- [x] 5.3 Колонки `себест_полн_ед` / `CM_ед` / `прибыль_ячейка` в выводе
  `forecast` (прибыль = маржа/ед × прогноз_спроса_сцен)
- [x] 5.4 Деградация в history/half-final-режиме (нет листов затрат) — пропуск
  колонок прибыли с предупреждением; `cost` требует валидный отчёт компании 1..8

## 6. Качество и документация

- [x] 6.1 `make validate` (ruff format + ruff check + mypy --strict) без ошибок
- [x] 6.2 Юнит-тесты: тождество сырья, баланс разнесения, EWMA-вырождение,
  деление на ноль в ценах ресурсов, оба выхода себестоимости
- [x] 6.3 Обновить `CLAUDE.md` (модуль `cost`, подкоманда, схемы парсера) и при
  необходимости `docs/FINDINGS.md` (модель сырья, нормативы, разнесение)
