## 1. Расширение схемы решений (`parser.py`)

- [x] 1.1 Дополнить `DEC_SCHEMA`/`parse_decisions` новыми позициями первого листа
  `'Your decisions'`: план поставок по 9 ячейкам (строки 23–25, столбцы продуктов
  5/7/9), время сборки по продуктам (row30, столбцы 5/7/9), число смен
  (row19, col22), найм/увольнение и часовая ставка сборщиков (row23/24, col15),
  покупка/продажа станков (row30, col15/col22). R&D/сайт/порты — позиции известны,
  но в v1 не выводятся (см. 2.6, не драйвят затраты)
- [x] 1.2 Валидировать новые позиции по всем 11 производственных отчётах: время
  сборки↔`worked_h` (точно во всех), смены↔реконструкция (точно), план↔`planned`;
  пустая ячейка → ключ опускается/None
- [x] 1.3 Тест: план поставок `parse_decisions` == `planned` по продуктам
  (`test_decisions_plan_equals_planned`)
- [x] 1.4 Тест: время сборки из решений × выпуск воспроизводит `assembler_worked_h`
  (≤1 %), константа мануала — нет (`test_decisions_assembly_time_reproduces_worked_h`)

## 2. Себестоимость от решений (`cost.py`)

- [x] 2.1 Объём ячейки в `_cell_frame` = план поставок из `parse_decisions`
  (фоллбэк `shipped`); материал per-unit — экономический (raw×spot×defect_infl),
  сверка материала улучшена +6.7 %→+2.0 %
- [x] 2.2 Цена ячейки из решений (`dec["price"]`) с фоллбэком на `W`
- [x] 2.3 Веса конверсии — на ФАКТИЧЕСКОМ времени сборки из решений (фоллбэк
  `ASSEMBLY_MIN`); станко-минуты `{60,75,120}` не тронуты
- [x] 2.4 Материал считается на объёме плана (не sold); конверсия/ОТК — фикс-пул
  от `cur.produced`, веса по плану
- [x] 2.5 Реклама → маркетинговые накладные из `adspend_total`×1000 (замена строки
  `реклама`); комиссии — из решений (`dist_comm`, фоллбэк `W`)
- [x] 2.6 Смены из решения (fallback реконструкция) — ГОТОВО; станки = текущие ±
  покупка/продажа → амортизация/`machine_opex` — ГОТОВО. **Труд-пул оставлен из
  отчёта** (`assembler+mechanic_salary`×absence_infl): реконструкция по ставке/
  штату шумная (±10 %), отчёт точнее и держит сверку → зарплата НЕ реагирует на
  правку ставки/найма (v1-ограничение, см. design). R&D/сайт/прочие накладные —
  из отчёта
- [x] 2.7 Полуфабрикаты (замещение станко-этапа) — из состояния отчёта
  (`cur.components_used` в `machine_hours`); ограничение в design

## 3. Две базы объёма продаж

- [x] 3.1 Себест/ед и CM/ед — на базе ПЛАНА, базонезависимы (аллокация overhead по
  плановой выручке); тест `test_forecast_base_columns_and_clip`
- [x] 3.2 База=план (дефолт): `прибыль_ячейка` = (цена−себест/ед)×план
- [x] 3.3 `cost(..., demand)`: продано = min(`спрос_сцен`, план); колонки
  `продано_прог`/`выручка_прог`/`прибыль_прог`; ячейка не в demand → база плана
- [x] 3.4 CLI-проверка покрытия CSV (<9 ячеек → предупреждение)

## 4. Интеграция и сверка

- [x] 4.1 УПРОЩЕНО: себест/ед базонезависима → `unit_costs` БЕЗ параметра
  `demand`; `forecast._add_cost_columns` уже умножает `full/cm` на `спрос_сцен` →
  `forecast.py` не менялся. Демо-база прогноза — через `cost(demand=...)`
- [x] 4.2 Двухрежимная сверка `pl_check` (`применима`/`режим`): `_plan_edited`
  (план решений vs `planned`, порог 2 %); тест `test_plan_edited_mode`
- [x] 4.3 Тест сверки на предзаполненном листе ≤5 % + режим=исполненные
  (`test_reconciliation_executed_within_threshold`)

## 5. CLI и вывод

- [x] 5.1 Флаг `--forecast <path>` в подкоманде `cost`; `_read_forecast_demand`
  (RU→EN, `спрос_сцен`)
- [x] 5.2 Колонки базы=план всегда; при `--forecast` — доп. колонки прогноза;
  строка режима сверки; `--out` дублирует CSV
- [x] 5.3 Обновить README.md/CLAUDE.md: `cost` от решений, `--forecast`, две базы,
  режимы сверки

## 6. Валидация

- [x] 6.1 `make validate` (ruff format + ruff check + mypy --strict + 48 тестов)
- [x] 6.2 Прогон `forecast → cost --forecast fc.csv` на `data/W115264.xls`:
  сверка +2.0 %, себест согласована с колонками forecast, база прогноза клипуется
- [x] 6.3 spec-дельта синхронизирована с реализацией (unit_costs/labor);
  `docs/FINDINGS.md` — при архивировании
